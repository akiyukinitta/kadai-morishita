$(function () {
	$(".slide-body").slick({
		console.log("hallo"),
    auto: true,
    // arrows: true,
    // slideWidth: 240,
    centerMode: true,
    centerPadding: 150,
    // slidesToShow: 4,
    // minSlides: 1,
    // maxSlides: 4,
    // moveSlides: 1,
    pause: 3000,
    // pager: arguments,
  });
});
